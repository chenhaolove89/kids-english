---
description: 快乐学园内容与题目审计循环：内容/出题改动必须走多子代理审计→修复→二轮复核→实测的门禁，零容错
alwaysApply: true
---

# 内容与题目审计循环（零容错门禁）

这是给孩子的学习产品，**任何错误都可能在教小孩错误的东西**。凡改动以下任一类内容，视为"内容批次"，合并/交付前必须走完整审计循环：

- 词表/识字表源：`tools/*.csv`（含 hanzi-emoji.csv）→ 生成的 `src/data/*.json`
- 出题逻辑：`src/domain/*`（mathgen/rounds/judge/progress）
- 题目渲染：`src/pages/quiz/*`、`src/pages/math/practice.vue`、`src/pages/learn/*`
- 静态资产：`src/static/**` 的增删改

## 循环步骤（不允许跳步、不允许抽样）

1. **并行子代理审计**（每科一个，Explore 只读）：英语 / 语文 / 数学+资产 各派一个，全量枚举。
2. **修复**全部 P0/P1；P2 择优。
3. **二轮复核子代理**（新开代理，不带修复者视角）：逐项验证修复真实生效 + 全新视角抓漏网。
4. **实测门禁**：`npm run test` + `npm run validate:content` + `npm run build:h5` 全绿，且浏览器实操走一遍触发过 bug 的场景（强刷缓存！`tab.reload()`）。

## 每科审计清单（累积自实战）

**通用**：引用断链（readdirSync **精确大小写**比对——Windows 不分区大小写会掩盖线上 404）；音频/图片文件存在且非损坏；文案错别字与口径一致；儿童内容安全（惊悚/暴力类逐条人工过目）。

**英语**：id 唯一、同 level en 不重复；en/zh/phonetic 与源 CSV 零漂移；语言混淆（en 含 CJK 等）；音标包裹与合理性；词图音三者配对；池大小 ≥10。

**语文**：id === char 码点（音频文件名按码点命名，错位=读错字音）；pinyin 用 pinyin-pro 逐字交叉验证（多音字单列人工确认）；例词必须含本字；**图标配例词而非字义**（大🐘配"大家"这类教训）——码点还原成 emoji 逐条人工过目；同图不同字要审混淆风险。

**数学**：每个题型跑 ≥2000 题统计验证：数值域守关卡名义（十以内/二十以内）、选项含答案/无重复/无 0/无负数、答案位置卡方均匀、除法整除、音频 seq 引用全部存在、快照 JSON 可序列化。

**题目模板（教训最重的一类）**：新增题型必须核对**渲染分支覆盖**——每个 kind 到达页面时必有可用分支与齐备字段（compareNum 曾因只修一处 v-if 而继续崩页，二轮复核才抓到）。用仓库内 vue 编译器或浏览器实测验证。

**会话/进度**：首答语义（跨暂停一致）、完成幂等、恢复不重放、attempts 裁剪不丢当前会话、restart 必须重建会话。

## 铁律

- 生成的 JSON（words/hanzi/catalog/audio-volumes）**禁止手改**——改源头 CSV 或工具后重跑管线（merge → gen:assets → gen-audio-volumes → build:content）。
- 修图标/资产码点后必须**删除旧文件**再重跑（existsSync 增量会跳过错图）。
- 每次审计输出必须含「已检查无问题项 + 覆盖规模」，无覆盖证明视为偷懒，退回重审。
- 审计发现用 `[P0/P1/P2] 位置 — 问题 — 证据` 格式登记；P0=孩子会直接学错/崩页，修复前不得交付。
