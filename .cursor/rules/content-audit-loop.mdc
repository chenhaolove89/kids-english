---
description: 快乐学园内容与题目审计循环：内容/出题改动必须走多子代理审计→修复→二轮复核→实测的门禁，零容错
alwaysApply: true
---

# 内容与题目审计循环（零容错门禁）

这是给孩子的学习产品，**任何错误都可能在教小孩错误的东西**。凡改动以下任一类内容，视为"内容批次"，合并/交付前必须走完整审计循环：

- 词表/识字表源：`tools/*.csv`（含 hanzi-emoji.csv）→ 生成的 `src/data/*.json`
- 出题逻辑：`src/domain/*`（mathgen/rounds/judge/progress）
- 题目渲染：`src/pages/quiz/*`、`src/pages/math/practice.vue`、`src/pages/learn/*`
- 静态资产：`src/static/**` 的增删改

## 循环步骤（不允许跳步、不允许抽样）

1. **并行子代理审计**（每科一个，Explore 只读）：英语 / 语文 / 数学+资产 各派一个，全量枚举。
2. **修复**全部 P0/P1；P2 择优。
3. **二轮复核子代理**（新开代理，不带修复者视角）：逐项验证修复真实生效 + 全新视角抓漏网。
4. **实测门禁**（全部要有退出码/实测输出，不接受"我看过了"）：
   - `npm run test`（纯函数/服务/存储/契约）、`npm run validate:content`、`npm run check:layering` 全绿
   - `npm run audit:assets`（引用断链、空文件、**截断**、音轨缺口）+ `npm run audit:audio`（解码全部 mp3：静音/过短/削波）
   - `npm run build:h5` 退出码 0
   - **浏览器实测**：`node tools/serve.mjs 4173` + `npm run smoke:h5`（真实 Chrome、**153 项断言**：12 个页面渲染与交互、答错讲解、会话快照/暂停/恢复/重练、口音切换与缺文件回退、返回键兜底、连点节流、路由守卫、低龄模式内容安全（含阴性对照）、描红按单字点亮、古诗点读记账、数学 9 关全题型、**运行期零 404**、零 console 报错）。触发过 bug 的场景必须强刷缓存再走一遍（`tab.reload()`）。若忘了起预览服务器，冒烟会 fail-fast 提示（不会白等十几分钟）。
   - **视觉回归**：改了页面结构/样式时用 `node tools/shots.mjs capture 基线` → 改 → `capture 对照` → `compare`；纯逻辑重构优先做新旧实现输入输出等价比对（像素只作辅助，工具有已文档化的绘制噪声）。
   - **发布前（改了构建 / service worker / 资源路径时必跑）**：`npm run build:h5` → `node tools/publish-github-pages.mjs` → `npm run verify:offline`。它跑的是**发布产物**（22 项）：SW 接管与外壳预缓存、**断网刷新仍能打开**、离线可听音（媒体元素真播放）、换代清旧缓存、**PWA manifest 完整性**（名字/`start_url`/`scope` 落在应用根、声明的图标在产物里存在、`index.html` 声明了标签页图标）、**发布产物运行期零 404**（跑 9 个页面 + 主动取运行时拼出来的音频/图片/笔顺数据）、以及**绝对路径改写完整性静态自检**（扫全部文本产物确认没有残留 `/static/` 字面量，并附带阳性对照：同一条规则在构建产物上必须命中 >100 处，否则说明规则本身失效）。
     为什么必须单独跑：service worker 与 `/static/` → `./static/` 的相对化**只存在于发布产物**；`smoke:h5` 跑的是构建目录，这两类问题它一个都测不到。
   - 本环境没有浏览器/设备时，明确写"未实测"并说明原因，不得用"应该没问题"代替。

## 新增内容时的清单（按内容类型查对应门禁）

| 新增了什么 | 必跑 | 要看到的证据 |
|---|---|---|
| 词/字/句（改 `tools/*.csv`） | `merge-words` → `gen:assets` → `build:content` → `validate:content` → `audit:assets` → `smoke:h5` | 目录课数与源一致（contentVersion 变化）、池大小 ≥10、图音字配对齐全 |
| 音频（TTS / 新口音） | `gen-audio-volumes`（**改音频后必须重跑**，否则响度表陈旧 → 播放响度偏差可达 8dB；2026-09 实测重跑后 3026 条里有 715 条增益变化）→ `audit:audio` → `smoke:h5` | 静音 / 疑似截断(<0.35s) / 削波 全 0；`audio-gb` 镜像齐全；口音切换真的取到 gb 音轨 |
| 图片 | `audit:assets` → `shots capture/compare` | 无孤儿、无空文件、无截断；页面截图无回归 |
| 新题型（`src/domain/*`） | `npm test`（题型集合自洽契约）→ `smoke:h5`（该关全部题型抽样作答） | 每题型「要么 4 选项、要么 2 张比大小卡」、题干非空、作答有反馈、无白屏 |
| 新学段 / 新课程 | `build:content` → `validate:content` → `smoke:h5` → `shots` 重建基线 | 阶段卡与入口可点、池非空（填空池要求每阶段不同字 ≥ 选项数）、内容不被低龄模式误伤 |
| 任何"只在发布产物才有"的东西（SW/相对化/manifest） | `publish-github-pages.mjs` → `verify:offline` | 22 项全绿（含断网可开、零 404、改写自检与阳性对照） |

> 写新断言时先问一句：**"这条断言会不会永远为真？"** 零长度负面断言（`x.length === 0`）、空数组 `.every()`、空来源的 `.filter()` 都会恒真。做法是同时断言"确实观察到了东西"（如 `seen.size >= 20`），或加一条阳性/阴性对照。

## 每科审计清单（累积自实战）

**通用**：引用断链（readdirSync **精确大小写**比对——Windows 不分区大小写会掩盖线上 404）；音频/图片文件存在且非损坏；文案错别字与口径一致；儿童内容安全（惊悚/暴力类逐条人工过目）。

**英语**：id 唯一、同 level en 不重复；en/zh/phonetic 与源 CSV 零漂移；语言混淆（en 含 CJK 等）；音标包裹与合理性；词图音三者配对；池大小 ≥10。

**语文**：id === char 码点（音频文件名按码点命名，错位=读错字音）；pinyin 用 pinyin-pro 逐字交叉验证（多音字单列人工确认）；例词必须含本字；**图标配例词而非字义**（大🐘配"大家"这类教训）——码点还原成 emoji 逐条人工过目；同图不同字要审混淆风险。

**数学**：每个题型跑 ≥2000 题统计验证：数值域守关卡名义（十以内/二十以内）、选项含答案/无重复/无 0/无负数、答案位置卡方均匀、除法整除、音频 seq 引用全部存在、快照 JSON 可序列化。

**题目模板（教训最重的一类）**：新增题型必须核对**渲染分支覆盖**——每个 kind 到达页面时必有可用分支与齐备字段（compareNum 曾因只修一处 v-if 而继续崩页，二轮复核才抓到）。用仓库内 vue 编译器或浏览器实测验证。

**会话/进度**：首答语义（跨暂停一致）、完成幂等、恢复不重放、attempts 裁剪不丢当前会话、restart 必须重建会话。

## 铁律

- 生成的 JSON（words/hanzi/catalog/audio-volumes）**禁止手改**——改源头 CSV 或工具后重跑管线（merge → gen:assets → gen-audio-volumes → build:content）。
- 修图标/资产码点后必须**删除旧文件**再重跑（existsSync 增量会跳过错图）。生成器只按**存在性**跳过是不够的：截断文件会被永久跳过，判定必须带体积阈值（见 `tools/lib/asset-check.mjs`）。
- **批处理文件时先确认是不是文本**：按扩展名白名单 + UTF-8 合法性校验，绝不把 mp3/zip/png 当文本读写（曾因此损坏 2 个 mp3 与一个 30MB 压缩包）。
- 修好一个缺陷后，尽量补一条能**复现该缺陷**的断言（单测或浏览器断言）；门禁要能自己发现问题，而不是靠人记得去点。
- 每次审计输出必须含「已检查无问题项 + 覆盖规模」，无覆盖证明视为偷懒，退回重审。
- 审计发现用 `[P0/P1/P2] 位置 — 问题 — 证据` 格式登记；P0=孩子会直接学错/崩页，修复前不得交付。
